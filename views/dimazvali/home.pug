doctype html
html(lang='ru')
  head
    meta(charset='utf-8')
    meta(name='viewport' content='width=device-width, initial-scale=1')
    title dimazvali.com
    meta(name='description' content='Дмитрий Шестаков — проджект-менеджер, фотограф, велосипедист')
    link(rel='icon' type='image/x-icon' href='/images/dimazvali/favicon.ico')
    link(rel='preconnect' href='https://fonts.googleapis.com')
    link(rel='preconnect' href='https://fonts.gstatic.com' crossorigin)
    link(href='https://fonts.googleapis.com/css2?family=Oswald:wght@300;400;500;600;700&family=Lora:ital,wght@0,400;0,500;0,600;1,400;1,500&family=JetBrains+Mono:wght@400;500;700&family=Noto+Sans+Georgian:wght@500;700&display=swap' rel='stylesheet')
    script(async src='https://www.googletagmanager.com/gtag/js?id=G-LDMN4XFB42')
    script.
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-LDMN4XFB42');
    style.
      *, *::before, *::after { box-sizing: border-box; }
      html, body { margin: 0; background: #f4f1ea; color: #15120f; font-family: 'JetBrains Mono', monospace; overflow-x: hidden; }
      a { text-decoration: none; color: inherit; }
      ::selection { background: #15120f; color: #f4f1ea; }

      /* ── Header ── */
      .site-header { max-width: 1320px; margin: 0 auto; display: flex; align-items: center; justify-content: space-between; gap: 16px; padding: 22px 28px; }
      .site-logo-wrap { position: relative; }
      .site-logo   { font-family: Oswald, sans-serif; font-weight: 700; font-size: 17px; letter-spacing: .02em; cursor: pointer; user-select: none; }
      .site-email  { font-size: 12px; letter-spacing: .08em; color: #56514a; border-bottom: 1px solid #c8c3b8; padding-bottom: 2px; }

      .logo-popup  { position: absolute; top: calc(100% + 10px); left: 0; width: 280px; background: #f4f1ea; border: 1px solid #c8c3b8; padding: 16px 18px; box-shadow: 0 4px 24px rgba(21,18,15,.13); opacity: 0; pointer-events: none; transform: translateY(-6px); transition: opacity .18s ease, transform .18s ease; z-index: 100; }
      .logo-popup.is-open { opacity: 1; pointer-events: auto; transform: translateY(0); }
      .logo-popup-head { font-size: 10px; letter-spacing: .15em; color: #9a948b; margin-bottom: 4px; text-transform: lowercase; }
      .logo-popup-word { font-family: Oswald, sans-serif; font-weight: 700; font-size: 22px; letter-spacing: .02em; line-height: 1; }
      .logo-popup-ipa  { font-size: 12px; color: #56514a; margin-top: 3px; font-style: italic; }
      .logo-popup-text { margin: 12px 0 0; font-size: 12px; line-height: 1.65; color: #56514a; }

      /* ── Hero ── */
      .hero            { max-width: 1320px; margin: 0 auto; padding: clamp(18px,2.5vw,36px) 28px clamp(34px,4.5vw,60px); display: flex; flex-wrap: wrap; gap: clamp(28px,4vw,60px); align-items: center; }
      .hero-text       { flex: 1 1 470px; min-width: 300px; }
      .hero-greeting   { font-family: Lora, serif; font-style: italic; font-size: clamp(30px,5vw,56px); line-height: 1; }
      #hero-name       { margin: 14px 0 0; font-family: Oswald, 'Noto Sans Georgian', sans-serif; font-weight: 700; font-size: clamp(46px,8.4vw,116px); line-height: .9; letter-spacing: -.012em; text-transform: uppercase; min-height: 1.8em; }
      .hero-tags       { margin-top: 24px; display: flex; gap: 9px; flex-wrap: wrap; font-size: 12.5px; letter-spacing: .06em; color: #56514a; }
      .hero-tag        { border: 1px solid #c8c3b8; padding: 6px 13px; }
      .hero-intro      { margin: 28px 0 0; max-width: 520px; font-family: Lora, serif; font-size: clamp(20px,2vw,27px); line-height: 1.42; }
      .hero-photo-col  { flex: 1 1 330px; min-width: 280px; position: relative; align-self: stretch; }
      .hero-portrait   { width: 100%; height: 100%; min-height: clamp(440px,58vh,640px); object-fit: cover; object-position: center 18%; display: block; background: #15120f; }
      .hero-city       { position: absolute; left: 0; bottom: 0; padding: 14px 16px; font-size: 11px; letter-spacing: .12em; color: #f4f1ea; background: linear-gradient(180deg, transparent, rgba(21,18,15,.7)); }

      /* ── Statement ── */
      .statement         { position: relative; background: #0c0b0a; }
      .statement-img     { width: 100%; height: clamp(360px,62vh,720px); object-fit: cover; object-position: center 38%; display: block; opacity: .92; }
      .statement-overlay { position: absolute; inset: 0; background: linear-gradient(90deg, rgba(12,11,10,.78) 0%, rgba(12,11,10,.32) 46%, rgba(12,11,10,0) 72%); }
      .statement-center  { position: absolute; inset: 0; display: flex; align-items: center; }
      .container         { max-width: 1320px; width: 100%; margin: 0 auto; padding: 0 28px; }
      .statement-quote   { margin: 0; max-width: 600px; }
      .statement-quote p { margin: 0; font-family: Lora, serif; font-style: italic; font-size: clamp(24px,3.4vw,44px); line-height: 1.28; color: #f4f1ea; }

      /* ── Directions ── */
      .directions        { max-width: 1320px; margin: 0 auto; padding: clamp(44px,6vw,90px) 28px clamp(24px,3vw,44px); }
      .directions-header { display: flex; align-items: baseline; gap: 14px; margin-bottom: 28px; }
      .directions-title  { margin: 0; font-family: Oswald, sans-serif; font-weight: 700; font-size: clamp(20px,2.6vw,30px); letter-spacing: .04em; text-transform: uppercase; }
      .directions-rule   { height: 1px; flex: 1; background: #d8d3c8; display: block; }
      .directions-label  { font-size: 11px; letter-spacing: .12em; color: #9a948b; }
      .directions-grid   { display: grid; grid-template-columns: repeat(auto-fit, minmax(340px,1fr)); gap: 16px; }

      /* ── Cards ── */
      .card-link               { display: block; position: relative; min-height: clamp(380px,46vw,520px); overflow: hidden; background: #15120f; transition: transform .35s ease; }
      .card-link:hover         { transform: translateY(-4px); }
      .card-img                { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; display: block; transition: transform .55s ease, opacity .35s ease; }
      .card-link:hover .card-img { transform: scale(1.04); opacity: .95 !important; }
      .card-img--photo         { object-position: center 30%; opacity: .82; }
      .card-img--it            { object-position: center 28%; opacity: .8; }
      .card-overlay            { position: absolute; inset: 0; background: linear-gradient(180deg, rgba(12,11,10,.15) 0%, rgba(12,11,10,.2) 45%, rgba(12,11,10,.9) 100%); }
      .card-overlay--it        { background: linear-gradient(180deg, rgba(12,11,10,.2) 0%, rgba(12,11,10,.25) 45%, rgba(12,11,10,.92) 100%); }
      .card-body               { position: absolute; inset: 0; padding: clamp(24px,3vw,38px); display: flex; flex-direction: column; justify-content: space-between; color: #f4f1ea; }
      .card-top                { display: flex; justify-content: space-between; align-items: flex-start; font-size: 12px; letter-spacing: .14em; }
      .card-meta               { opacity: .8; }
      .card-arrow              { font-size: 20px; line-height: 1; }
      .card-keyword            { font-family: Oswald, sans-serif; font-weight: 700; font-size: clamp(44px,7vw,82px); line-height: .92; text-transform: uppercase; }
      .card-domain             { font-size: clamp(13px,1.5vw,17px); letter-spacing: .06em; color: #b7b2aa; margin-top: 6px; }
      .card-desc               { margin: 12px 0 0; font-family: Lora, serif; font-size: 16px; line-height: 1.55; color: #dedad3; max-width: 420px; }

      /* ── Footer ── */
      .site-footer          { background: #15120f; color: #f4f1ea; margin-top: clamp(40px,6vw,80px); background-image: url(https://storage.googleapis.com/photo-dimazvalimisc/images/ks-05287-1781288494378-2400.webp); background-size: cover; background-position: 50%; }
      .footer-top           { max-width: 1320px; margin: 0 auto; padding: clamp(40px,5vw,68px) 28px; display: flex; flex-wrap: wrap; gap: 32px; align-items: flex-end; justify-content: space-between; }
      .footer-contact-label { font-size: 12px; letter-spacing: .3em; text-transform: uppercase; color: #7c776f; }
      .footer-contact-title { margin-top: 14px; font-family: Oswald, sans-serif; font-weight: 600; font-size: clamp(26px,4vw,46px); line-height: 1.05; letter-spacing: .01em; }
      .footer-links         { display: flex; flex-direction: column; gap: 10px; font-size: 14px; letter-spacing: .03em; }
      .footer-link          { display: flex; gap: 14px; }
      .footer-link-label    { color: #7c776f; width: 78px; }
      .footer-bottom-wrap   { border-top: 1px solid #2a2622; }
      .footer-bottom        { max-width: 1320px; margin: 0 auto; padding: 16px 28px; display: flex; justify-content: space-between; flex-wrap: wrap; gap: 10px; font-size: 11px; letter-spacing: .08em; color: #6c675f; }

      /* ── Globe ── */
      .globe-section        { max-width: 1320px; margin: 0 auto; padding: clamp(44px,6vw,90px) 28px clamp(44px,6vw,80px); }
      .globe-wrap           { position: relative; display: flex; justify-content: center; align-items: flex-start; }
      .globe-svg            { display: block; cursor: grab; flex-shrink: 0; }
      .globe-svg:active     { cursor: grabbing; }
      .globe-sphere         { fill: #f4f1ea; }
      .globe-ring           { fill: none; stroke: #c8c3b8; stroke-width: 1; }
      .globe-grat           { fill: none; stroke: #ddd; stroke-width: .3; }
      .globe-country        { fill: #e4e0d8; stroke: #c0bab2; stroke-width: .4; }
      .globe-visited        { fill: #15120f; stroke: none; }
      .globe-list           { position: absolute; right: 0; top: 50%; transform: translateY(-50%); background: #f4f1ea; border: 1px solid #d8d3c8; padding: 12px 0; min-width: 164px; }
      .globe-list-label     { font-size: 9px; letter-spacing: .18em; text-transform: uppercase; color: #9a948b; padding: 0 16px 8px; border-bottom: 1px solid #e0dcd2; margin-bottom: 4px; display: block; }
      .globe-list-item      { display: block; width: 100%; background: none; border: none; border-left: 2px solid transparent; font-family: 'JetBrains Mono', monospace; font-size: 12px; padding: 6px 16px; color: #56514a; cursor: pointer; text-align: left; }
      .globe-list-item:hover { color: #15120f; }
      .globe-list-item.active { border-left-color: #15120f; color: #15120f; }
      @media (max-width: 720px) {
        .globe-wrap       { flex-direction: column; align-items: center; }
        .globe-list       { position: static; transform: none; width: 100%; margin-top: 20px; display: flex; flex-wrap: wrap; gap: 0; }
        .globe-list-label { width: 100%; }
        .globe-list-item  { width: auto; flex: 0 0 auto; }
      }

      @media (prefers-reduced-motion: reduce) {
        .card-link, .card-link:hover { transform: none; }
        .card-img { transition: none; }
      }

  body
    header.site-header
      .site-logo-wrap
        span.site-logo(role='button' tabindex='0' aria-expanded='false') dimazvali.com
        .logo-popup(aria-hidden='true')
          .logo-popup-head читается как
          .logo-popup-word димацвали
          .logo-popup-ipa [dɪmɑtsvɑlɪ]
          p.logo-popup-text так уж вышло, что ещё школьное прозвище было с грузиским акцентом. и только через много лет оказалось, что генацвале пишется через ე. то ли школа была средняя, то ли дети нелюбопытные
      a.site-email(href='mailto:dimazvali@gmail.com')
        | dimazvali
        span @
        | gmail.com

    section.hero
      .hero-text
        #geo-greeting.hero-greeting გამარჯობა!
        h1#hero-name
          | Дмитрий
          br
          | Шестаков
        .hero-tags
          each tag in ['проджект-менеджер', 'фотограф', 'велосипедист']
            span.hero-tag= tag
        p.hero-intro
          | Вообще-то я проджект-менеджер из&nbsp;Тбилиси. Но в&nbsp;частности — фотограф из Петербурга. А то и вовсе велосипедист.&nbsp;
          em По обстоятельствам.
      .hero-photo-col
        img.hero-portrait(
          src='https://storage.googleapis.com/photo-dimazvalimisc/dimazvali/home/ks-05065-800.webp'
          srcset='https://storage.googleapis.com/photo-dimazvalimisc/dimazvali/home/ks-05065-800.webp 800w, https://storage.googleapis.com/photo-dimazvalimisc/dimazvali/home/ks-05065-2400.webp 2400w'
          sizes='(max-width:900px) 100vw, 50vw'
          alt='Дмитрий Шестаков. Фото: Катя Снигиревская'
        )
        .hero-city ТБИЛИСИ

    section.statement
      img.statement-img(
        src='https://storage.googleapis.com/photo-dimazvalimisc/dimazvali/home/ks-05295-800.webp'
        srcset='https://storage.googleapis.com/photo-dimazvalimisc/dimazvali/home/ks-05295-800.webp 800w, https://storage.googleapis.com/photo-dimazvalimisc/dimazvali/home/ks-05295-2400.webp 2400w'
        sizes='100vw'
        alt=''
      )
      .statement-overlay
      .statement-center
        .container
          blockquote.statement-quote
            p На самом деле, пиарщик. Но не по-настоящему. Только по&nbsp;диплому СПбГУ.

    section.directions
      .directions-header
        h2.directions-title Куда дальше
        span.directions-rule
        span.directions-label два мира
      .directions-grid
        a.card-link(href='https://photo.dimazvali.com')
          img.card-img.card-img--photo(
            src='https://storage.googleapis.com/photo-dimazvalimisc/dimazvali/home/ks-04803-800.webp'
            srcset='https://storage.googleapis.com/photo-dimazvalimisc/dimazvali/home/ks-04803-800.webp 800w, https://storage.googleapis.com/photo-dimazvalimisc/dimazvali/home/ks-04803-2400.webp 2400w'
            sizes='(max-width:700px) 100vw, 50vw'
            alt=''
          )
          .card-overlay
          .card-body
            .card-top
              span.card-meta 01 — ФОТОГРАФИЯ
              span.card-arrow →
            div
              .card-keyword Фото
              .card-domain photo.dimazvali.com
              p.card-desc Документальные чёрно-белые серии, портреты, репортаж. Грузия, Казахстан, Черногория.

        a.card-link(href='https://it.dimazvali.com')
          img.card-img.card-img--it(
            src='https://storage.googleapis.com/photo-dimazvalimisc/dimazvali/home/ks-04770-800.webp'
            srcset='https://storage.googleapis.com/photo-dimazvalimisc/dimazvali/home/ks-04770-800.webp 800w, https://storage.googleapis.com/photo-dimazvalimisc/dimazvali/home/ks-04770-2400.webp 2400w'
            sizes='(max-width:700px) 100vw, 50vw'
            alt=''
          )
          .card-overlay.card-overlay--it
          .card-body
            .card-top
              span.card-meta 02 — IT & ПРОДЖЕКТ-МЕНЕДЖМЕНТ
              span.card-arrow →
            div
              .card-keyword IT&nbsp;/&nbsp;PM
              .card-domain it.dimazvali.com
              p.card-desc Истории про продукты, команды и&nbsp;разработку. Как доводить сложное до&nbsp;запуска.

    //- -
    //-   var globeCountries = [
    //-     // Кавказ и СНГ
    //-     { name: 'Грузия',              id: 268 },
    //-     { name: 'Армения',             id: 51  },
    //-     { name: 'Азербайджан',         id: 31  },
    //-     { name: 'Россия',              id: 643 },
    //-     { name: 'Казахстан',           id: 398 },
    //-     { name: 'Украина',             id: 804 },
    //-     { name: 'Беларусь',            id: 112 },
    //-     { name: 'Молдова',             id: 498 },
    //-     // Балканы
    //-     { name: 'Турция',              id: 792 },
    //-     { name: 'Сербия',              id: 688 },
    //-     { name: 'Черногория',          id: 499 },
    //-     { name: 'Хорватия',            id: 191 },
    //-     { name: 'Босния',              id: 70  },
    //-     { name: 'Северная Македония',  id: 807 },
    //-     { name: 'Албания',             id: 8   },
    //-     { name: 'Болгария',            id: 100 },
    //-     { name: 'Румыния',             id: 642 },
    //-     { name: 'Греция',              id: 300 },
    //-     { name: 'Кипр',                id: 196 },
    //-     // Центральная Европа
    //-     { name: 'Польша',              id: 616 },
    //-     { name: 'Чехия',               id: 203 },
    //-     { name: 'Словакия',            id: 703 },
    //-     { name: 'Словения',            id: 705 },
    //-     { name: 'Венгрия',             id: 348 },
    //-     { name: 'Австрия',             id: 40  },
    //-     { name: 'Швейцария',           id: 756 },
    //-     { name: 'Лихтенштейн',         id: 438 },
    //-     // Западная Европа
    //-     { name: 'Германия',            id: 276 },
    //-     { name: 'Нидерланды',          id: 528 },
    //-     { name: 'Бельгия',             id: 56  },
    //-     { name: 'Люксембург',          id: 442 },
    //-     { name: 'Франция',             id: 250 },
    //-     { name: 'Монако',              id: 492 },
    //-     { name: 'Андорра',             id: 20  },
    //-     { name: 'Испания',             id: 724 },
    //-     { name: 'Португалия',          id: 620 },
    //-     { name: 'Италия',              id: 380 },
    //-     { name: 'Сан-Марино',          id: 674 },
    //-     // Северная Европа
    //-     { name: 'Финляндия',           id: 246 },
    //-     { name: 'Эстония',             id: 233 },
    //-     { name: 'Латвия',              id: 428 },
    //-     { name: 'Литва',               id: 440 },
    //-     { name: 'Дания',               id: 208 },
    //-     { name: 'Швеция',              id: 752 },
    //-     { name: 'Норвегия',            id: 578 },
    //-     { name: 'Исландия',            id: 352 },
    //-     { name: 'Ирландия',            id: 372 },
    //-     { name: 'Великобритания',      id: 826 },
    //-     { name: 'Мальта',              id: 470 },
    //-     // Северная Африка
    //-     { name: 'Марокко',             id: 504 },
    //-     { name: 'Алжир',               id: 12  },
    //-     { name: 'Тунис',               id: 788 },
    //-     { name: 'Ливия',               id: 434 },
    //-     { name: 'Египет',              id: 818 },
    //-   ]
    //- section.globe-section
    //-   .directions-header
    //-     h2.directions-title Где был
    //-     span.directions-rule
    //-     span.directions-label= globeCountries.length + ' стран и территорий'
    //-   .globe-wrap#globe-container
    //-     svg#globe-svg
    //-     .globe-list
    //-       span.globe-list-label Страны
    //-       each c in globeCountries
    //-         button.globe-list-item(data-id=String(c.id) type='button')= c.name

    footer.site-footer
      .footer-top
        div
          .footer-contact-label Связаться
          .footer-contact-title Напишите мне
        .footer-links
          a.footer-link(href='mailto:dimazvali@gmail.com')
            span.footer-link-label email
            span dimazvali@gmail.com
          a.footer-link(href='https://telegram.me/dimazvali')
            span.footer-link-label telegram
            span @dimazvali
          a.footer-link(href='https://instagram.com/dimazvali2')
            span.footer-link-label instagram
            span @dimazvali2
      .footer-bottom-wrap
        .footer-bottom
          span © dimazvali.com
          span#footer-time გამარჯობა!

    script.
      (function() {
        // Logo popup
        var logo  = document.querySelector('.site-logo');
        var popup = document.querySelector('.logo-popup');
        if (logo && popup) {
          logo.addEventListener('click', function(e) {
            e.stopPropagation();
            var open = popup.classList.toggle('is-open');
            logo.setAttribute('aria-expanded', open);
            popup.setAttribute('aria-hidden', !open);
          });
          logo.addEventListener('keydown', function(e) {
            if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); logo.click(); }
            if (e.key === 'Escape') { popup.classList.remove('is-open'); logo.setAttribute('aria-expanded', 'false'); }
          });
          document.addEventListener('click', function() {
            popup.classList.remove('is-open');
            logo.setAttribute('aria-expanded', 'false');
            popup.setAttribute('aria-hidden', 'true');
          });
        }

        // Tbilisi time (UTC+4)
        function updateTbilisiTime() {
          var now = new Date(Date.now() + 4 * 3600000);
          var h = now.getUTCHours();
          var m = now.getUTCMinutes();
          var hh = String(h).padStart(2, '0');
          var mm = String(m).padStart(2, '0');
          var time = hh + ':' + mm;
          var ft = document.getElementById('footer-time');
          if (ft) ft.textContent = h < 12 ? 'учтите, у меня ' + time + ' утра' : 'учтите, у меня ' + time;
        }
        updateTbilisiTime();
        setInterval(updateTbilisiTime, 60000);

        // Hero greeting (set once)
        (function() {
          var h = new Date(Date.now() + 4 * 3600000).getUTCHours();
          var g = h >= 6 && h < 12 ? 'დილა მშვიდობისა'
                : h >= 12 && h < 18 ? 'შუადღე მშვიდობისა'
                : h >= 18 && h < 23 ? 'საღამო მშვიდობისა'
                : 'ღამე მშვიდობისა';
          document.getElementById('geo-greeting').textContent = g;
        }());

        // Name scramble animation
        var el = document.getElementById('hero-name');
        if (!el || window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;

        var variants = [
          ['Дмитрий', 'Шестаков'],
          ['Dmitry',  'Shestakov'],
          ['დმიტრი',  'შესტაკოვი'],
        ];
        var pool = 'абвгдежзийклмнопрстухцчшщыэ' + 'abcdefghijkmnopqrstuvxyz' + 'აბგდევზთიკლმნოპრსტუფქღშჩცძწჭხჯჰ';

        var idx = 0;
        var current = variants[0];
        var queues, frame, raf;

        function rand() { return pool[Math.floor(Math.random() * pool.length)]; }

        function setText(newPair) {
          var oldPair = current;
          current = newPair;
          queues = [0, 1].map(function(w) {
            var from = oldPair[w] || '', to = newPair[w] || '';
            var len = Math.max(from.length, to.length), q = [];
            for (var i = 0; i < len; i++) {
              var start = Math.floor(Math.random() * 22);
              q.push({ fromC: from[i] || '', toC: to[i] || '', start: start, end: start + 12 + Math.floor(Math.random() * 24), char: null });
            }
            return q;
          });
          frame = 0;
          cancelAnimationFrame(raf);
          update();
        }

        function update() {
          var done = true;
          var out = queues.map(function(q) {
            var s = '';
            for (var i = 0; i < q.length; i++) {
              var it = q[i];
              if (frame >= it.end) {
                s += it.toC;
              } else if (frame >= it.start) {
                done = false;
                if (it.char === null || Math.random() < 0.3) it.char = rand();
                s += '<span style="opacity:.5;font-weight:500">' + it.char + '</span>';
              } else {
                done = false;
                s += it.fromC;
              }
            }
            return s;
          });
          el.innerHTML = out[0] + '<br>' + out[1];
          frame++;
          if (!done) raf = requestAnimationFrame(update);
        }

        setInterval(function() {
          idx = (idx + 1) % variants.length;
          setText(variants[idx]);
        }, 3400);
      }());
    //- script(src='https://cdn.jsdelivr.net/npm/d3@7/dist/d3.min.js' defer)
    //- script(src='https://cdn.jsdelivr.net/npm/topojson-client@3/dist/topojson-client.min.js' defer)
    //- script.
    //-   document.addEventListener('DOMContentLoaded', function() {
    //-     var COUNTRIES = !{JSON.stringify(globeCountries)};
    //-     var VISITED_IDS = new Set(COUNTRIES.map(function(c) { return c.id; }));

    //-     var container = document.getElementById('globe-container');
    //-     if (!container || typeof d3 === 'undefined' || typeof topojson === 'undefined') return;

    //-     var size = Math.min(container.clientWidth - 200, 480);
    //-     if (size < 220) size = 220;
    //-     var r = size / 2 - 4;

    //-     var svg = d3.select('#globe-svg')
    //-       .attr('width', size)
    //-       .attr('height', size);

    //-     var projection = d3.geoOrthographic()
    //-       .scale(r)
    //-       .translate([size / 2, size / 2])
    //-       .clipAngle(90)
    //-       .rotate([15, -42]);

    //-     var pathGen = d3.geoPath().projection(projection);

    //-     svg.append('circle')
    //-       .attr('class', 'globe-sphere')
    //-       .attr('cx', size / 2).attr('cy', size / 2).attr('r', r);

    //-     var gratPath = svg.append('path').attr('class', 'globe-grat');

    //-     var landGroup = svg.append('g');

    //-     svg.append('circle')
    //-       .attr('class', 'globe-ring')
    //-       .attr('cx', size / 2).attr('cy', size / 2).attr('r', r);

    //-     function render() {
    //-       gratPath.attr('d', pathGen(d3.geoGraticule()()));
    //-       landGroup.selectAll('path').attr('d', pathGen);
    //-     }

    //-     fetch('https://cdn.jsdelivr.net/npm/world-atlas@2/countries-110m.json')
    //-       .then(function(res) { return res.json(); })
    //-       .then(function(world) {
    //-         var features = topojson.feature(world, world.objects.countries).features;

    //-         var centroids = {};
    //-         features.forEach(function(f) { centroids[+f.id] = d3.geoCentroid(f); });

    //-         landGroup.selectAll('path')
    //-           .data(features)
    //-           .enter().append('path')
    //-           .attr('class', function(d) {
    //-             return VISITED_IDS.has(+d.id) ? 'globe-country globe-visited' : 'globe-country';
    //-           })
    //-           .attr('d', pathGen);

    //-         render();

    //-         var dragOrigin = null;
    //-         svg.call(d3.drag()
    //-           .on('start', function(event) {
    //-             dragOrigin = { x: event.x, y: event.y, r: projection.rotate().slice() };
    //-           })
    //-           .on('drag', function(event) {
    //-             if (!dragOrigin) return;
    //-             projection.rotate([
    //-               dragOrigin.r[0] + (event.x - dragOrigin.x) * 0.35,
    //-               dragOrigin.r[1] - (event.y - dragOrigin.y) * 0.35
    //-             ]);
    //-             render();
    //-           })
    //-         );

    //-         document.querySelectorAll('.globe-list-item').forEach(function(btn) {
    //-           btn.addEventListener('click', function() {
    //-             var id = +btn.dataset.id;
    //-             var centroid = centroids[id];
    //-             if (!centroid) return;

    //-             document.querySelectorAll('.globe-list-item').forEach(function(b) {
    //-               b.classList.remove('active');
    //-             });
    //-             btn.classList.add('active');

    //-             var from = projection.rotate().slice();
    //-             var to = [-centroid[0], -centroid[1]];
    //-             var interp = d3.interpolate(from, to);

    //-             d3.transition().duration(800).ease(d3.easeCubicInOut)
    //-               .tween('rotate', function() {
    //-                 return function(t) {
    //-                   projection.rotate(interp(t));
    //-                   render();
    //-                 };
    //-               });
    //-           });
    //-         });
    //-       });
    //-   });
