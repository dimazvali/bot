doctype html
html
  head
    title admin
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    meta(type='description' content='Истории, которые хочется слушать.')
    script(src='/javascripts/axios.js')
    script(src='/javascripts/common.js')
    script(src='/javascripts/web.js')
    link(rel='stylesheet', href='/stylesheets/style.css')
    link(rel="stylesheet", href="https://fonts.googleapis.com/css2?family=Oswald:wght@300;400;600&display=swap")
    style.
      :root { color-scheme: dark; }
      body {
        margin: 0;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: #0f1115;
        color: #e6e6e6;
        font-family: 'Oswald', sans-serif;
        letter-spacing: 0.5px;
      }
      #centered {
        text-align: center;
        padding: 32px 24px;
        border: 1px solid #1f2430;
        border-radius: 12px;
        background: rgba(255, 255, 255, 0.02);
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.45);
      }
      h1 {
        margin: 0 0 18px;
        font-weight: 400;
        text-transform: uppercase;
        font-size: 22px;
      }
    
  body.mobile.start
    #centered
      h1 let's play?
      script(src='https://telegram.org/js/telegram-widget.js?22' data-size="large" data-onauth="onTelegramAuth(user)" data-telegram-login="dimazvalibot" onerror="document.getElementById('tg-script-error-auth').style.display='block'")
      p#tg-script-error-auth(style="display:none; color:#e14; margin-top:.5em;") к сожалению, доступ к телеграм из вашей сети закрыт. воспользуйтесь авторизацией через почту
