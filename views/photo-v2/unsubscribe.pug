extends layout

block content
  .about-page
    if ok
      h1.hero-title= ui.unsubDoneTitle
      p.about-text= ui.unsubDoneText
    else
      h1.hero-title= ui.unsubErrTitle
      p.about-text= ui.unsubErrText
    a.btn.btn--line(href=lang === 'en' ? '/en' : '/')= ui.backHome
