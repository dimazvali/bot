mixin pageNav(items, sectionTitle)
  if items && items.length > 1
    nav.page-nav
      span.page-nav-label #{sectionTitle || items[0].label}
      .page-nav-scroll
        each item in items
          a.page-nav-btn(href=item.href) #{item.label}
