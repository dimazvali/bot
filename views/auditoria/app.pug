
doctype html
html
  head
    title Auditoria
    script(src='https://telegram.org/js/telegram-web-app.js')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    meta(type='description' content='Попробуйте — вам понравится.')
    script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/0.20.0/axios.js')
    script!=`userId="${user}"`
    script!=`intention=${start ? `"${start}"` : false }`
    script!=`translations = ${JSON.stringify(translations)}`
    script(src='/javascripts/common.js')
    script!=`${start?`let start="${start}"`:'let start=null'}`
    link(rel='stylesheet', href='/stylesheets/au/style2.css')
    link(rel="preconnect", href="https://fonts.googleapis.com")
    link(rel="preconnect", href="https://fonts.gstatic.com")
    link(rel="stylesheet", href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700;900&family=Rubik+80s+Fade&display=swap")
  body
    #header Auditoria Books Bar
    #links
      button Кабинет
      button Лекции
      button Бар
      button Коворкинг
      button Концерты
    #logo
      img(src="/images/auditoria/logo.svg", alt="")
    script(src='/javascripts/au/app.js')
