
doctype html
html
  head
    title!=(name ? `${name} | ` : '')+  `books.dimazvali.com`
    meta(property='og:title' content!=(name || `books.dimazvali.com`))
    
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    
    meta(name='description' content!=(description || 'Давайте дружить.'))
    meta(property='og:description' content!=(description || 'Давайте дружить.'))
    block head 
    link(rel='icon', type="image/x-icon", href='/images/books/favicon.ico')
    block style
    if (image)
      meta(property='og:image' content!=image)
    
    script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/0.20.0/axios.js')
    script(src='/javascripts/common.js')
    //- script(src='/javascripts/books/web.js')
    link(rel='stylesheet', href='/stylesheets/books/web.css')
    link(rel="preconnect", href="https://fonts.googleapis.com")
    link(rel="preconnect", href="https://fonts.gstatic.com")
    link(rel="stylesheet", href="https://fonts.googleapis.com/css2?family=Oi:wght@100;200;400;700;900&family=Oswald:wght@200;400;700;900&display=swap")
  body(class!=light?`light`:'')
    //- a.title(href="/") Нева FM
    block back
    //- .flex.rev
    
    #content.f
      block content
    //- block player 
      
  include footer