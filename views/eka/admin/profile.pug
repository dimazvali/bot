doctype html
html
  head
    title= title
    meta(charset='utf-8')
    link(rel='stylesheet' href='/stylesheets/eka/style.css')
  body.admin-body
    include _nav
    .admin-content
      .admin-title Профиль — Эка Елисеева
      form.admin-form(method='POST' action='/admin/profile' enctype='multipart/form-data')
        .admin-field
          label Фото (загрузить новое)
          input(type='file' name='photo' accept='image/*')
          if profile && profile.photoSizes
            p.admin-hint(style='margin-top:8px') Текущее фото:
            img(src=profile.photoSizes.w400 style='max-width:200px;margin-top:4px;border-radius:2px')
        .admin-field
          label Подпись в шапке (RU) — короткий текст под именем на главной
          input(type='text' name='taglineRu' value=profile && profile.taglineRu || '' placeholder='Живу в Грузии 12 лет. Показываю страну...')
        .admin-field
          label Tagline (EN)
          input(type='text' name='taglineEn' value=profile && profile.taglineEn || '' placeholder='I have lived in Georgia for 12 years...')
        .admin-field
          label Биография (RU)
          textarea(name='bioRu' style='min-height:200px')= profile && profile.bioRu || ''
          p.admin-hint Абзацы разделяйте двойным переносом строки.
        .admin-field
          label Biography (EN)
          textarea(name='bioEn' style='min-height:200px')= profile && profile.bioEn || ''

        h3(style='font-size:14px;font-weight:500;margin:24px 0 16px;border-bottom:1px solid #e8e8e8;padding-bottom:8px') Контакты
        .admin-2col
          .admin-field
            label Telegram
            input(type='text' name='telegram' value=profile && profile.telegram || '' placeholder='https://t.me/username')
          .admin-field
            label WhatsApp
            input(type='text' name='whatsapp' value=profile && profile.whatsapp || '' placeholder='https://wa.me/995500000000')
        .admin-2col
          .admin-field
            label Instagram
            input(type='text' name='instagram' value=profile && profile.instagram || '' placeholder='https://instagram.com/username')
          .admin-field
            label Email
            input(type='email' name='contactEmail' value=profile && profile.contactEmail || '' placeholder='eka@example.com')

        .admin-field
          button.admin-btn(type='submit') Сохранить
    script(src='/javascripts/eka/admin.js')
