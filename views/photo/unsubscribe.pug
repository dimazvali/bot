extends layout

block content
  .about-page
    if ok
      h2.about-title= ui.unsubDoneTitle
      p.about-text= ui.unsubDoneText
    else
      h2.about-title= ui.unsubErrTitle
      p.about-text= ui.unsubErrText
    a.about-link(href=lang === 'en' ? '/en' : '/')= ui.backHome
