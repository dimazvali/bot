doctype html
html(lang='ru')
  head
    meta(charset='utf-8')
    meta(name='viewport' content='width=device-width, initial-scale=1')
    title it.dimazvali.com
    link(rel='icon' type='image/x-icon' href='/images/dimazvali/favicon.ico')
    link(rel='preconnect' href='https://fonts.googleapis.com')
    link(rel='preconnect' href='https://fonts.gstatic.com' crossorigin)
    link(href='https://fonts.googleapis.com/css2?family=Oswald:wght@700&family=JetBrains+Mono:wght@400&display=swap' rel='stylesheet')
    style.
      *, *::before, *::after { box-sizing: border-box; }
      html, body { margin: 0; height: 100%; background: #f4f1ea; color: #15120f; font-family: 'JetBrains Mono', monospace; }
      body { display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: 100vh; text-align: center; padding: 24px; }
      ::selection { background: #15120f; color: #f4f1ea; }
  body
    div(style='font-family: Oswald, sans-serif; font-weight: 700; font-size: clamp(120px, 30vw, 380px); line-height: 1; letter-spacing: -.03em; color: #15120f; user-select: none;') 418
    div(style='margin-top: 16px; font-size: clamp(12px, 2vw, 18px); letter-spacing: .2em; color: #56514a; text-transform: lowercase;') тот еще вразработчик
    a(href='https://dimazvali.com' style='margin-top: 48px; font-size: 11px; letter-spacing: .1em; color: #9a948b; border-bottom: 1px solid #c8c3b8; padding-bottom: 2px; text-decoration: none;') ← dimazvali.com
