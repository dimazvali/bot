//- SEO — JSON-LD structured data (schema.org) + breadcrumb styling.
//- `jsonLd` is built per-route; absent on pages that don't need it.
if typeof jsonLd !== 'undefined' && jsonLd
  script(type='application/ld+json')!= JSON.stringify(jsonLd).replace(/</g, '\\u003c')
style.
  .te-crumbs{display:flex;flex-wrap:wrap;gap:6px;align-items:center;padding:11px 16px;font-size:12px;line-height:1.4;color:var(--muted,#6f665c);border-bottom:1px solid var(--line,#ded4c4);background:var(--panel,#f2ebe0);font-family:var(--sans,system-ui,sans-serif)}
  .te-crumbs a{color:inherit;text-decoration:none}
  .te-crumbs a:hover{color:var(--acc,#e2402a)}
  .te-crumbs .sep{color:#c0b5a4}
  .te-crumbs [aria-current]{color:var(--ink,#14110e);font-weight:600}
  @media(min-width:1000px){.te-crumbs{padding:12px 40px}}
